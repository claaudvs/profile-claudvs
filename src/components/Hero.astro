---
import Card from "./Card.astro";
import AvailableStatus from "./core/AvailableStatus.astro";
import SocialLinks from "./core/SocialLinks.astro";

const roles = [
  "Frontend Developer",
  "Open Source Contributor",
  "TypeScript Enthusiast",
];
const description =
  "Hi, I'm Claud! I build web applications and contribute to open source projects. I have a passion for TypeScript and love sharing knowledge through blogging.";
---

<section id="home" class="relative max-h-screen flex pt-14 overflow-hidden">
  <Card className="w-full h-auto" title="Hello 🍕" description={description}>
    <SocialLinks />
    <AvailableStatus />

    <div
      class="bg-zinc-50 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-6 font-mono text-sm space-y-2"
    >
      <p class="text-zinc-400 dark:text-zinc-500">// quick facts</p>
      <p>
        <span class="text-accent">const</span>
        <span class="text-zinc-700 dark:text-zinc-300">location =</span>
        <span class="text-emerald-600 dark:text-emerald-400">"Remote"</span>;
      </p>
      <p>
        <span class="text-accent">const</span>
        <span class="text-zinc-700 dark:text-zinc-300">experience =</span>
        <span class="text-emerald-600 dark:text-emerald-400">"5+ years"</span>;
      </p>
      <p>
        <span class="text-accent">const</span>
        <span class="text-zinc-700 dark:text-zinc-300">focus =</span>
        <span class="text-emerald-600 dark:text-emerald-400"
          >"Web Development"</span
        >;
      </p>
      <p>
        <span class="text-accent">const</span>
        <span class="text-zinc-700 dark:text-zinc-300">available =</span>
        <span class="text-blue-500 dark:text-blue-400">true</span>;
      </p>
    </div>
  </Card>
</section>

<script>
  const roles = [
    "Full-Stack Developer",
    "Open Source Contributor",
    "TypeScript Enthusiast",
  ];
  const el = document.getElementById("hero-role");
  let i = 0;

  setInterval(() => {
    if (!el) return;
    el.style.opacity = "0";
    el.style.transform = "translateY(-8px)";
    setTimeout(() => {
      i = (i + 1) % roles.length;
      el.textContent = roles[i];
      el.style.opacity = "1";
      el.style.transform = "translateY(0)";
    }, 300);
  }, 3000);
</script>

<style>
  #hero-role {
    transition:
      opacity 0.3s ease,
      transform 0.3s ease;
  }
</style>
